package model.work;

import model.base.WorkBaseViewPage;
import org.openqa.selenium.WebDriver;

public class EntityViewPage extends WorkBaseViewPage {

    public EntityViewPage(WebDriver driver) {
        super(driver);
    }
}