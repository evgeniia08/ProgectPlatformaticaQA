package model.base;

import org.openqa.selenium.WebDriver;

public abstract class WorkBaseViewPage extends WorkBaseIndexPage{

    public WorkBaseViewPage(WebDriver driver) {
        super(driver);
    }
}
